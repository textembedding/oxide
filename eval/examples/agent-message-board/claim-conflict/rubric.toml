expected_approval = false
minimum_unresolved = 1
required_signal_groups = [["claim", "BoardSeq", "principal"]]
forbidden_terms = ["Kafka", "Redis", "Kubernetes", "PostgreSQL"]
fixture_unresolved = ["Claim winner semantics conflict: PRODUCT.md selects the earliest valid Claim by BoardSeq, while CLAIM-CONFLICT.md selects the lexicographically smallest requested owner principal even when its BoardSeq is greater."]

[[requirements]]
id = "immutable-record"
path = "PRODUCT.md"
anchor = "3.4 Record"
text = "No operation edits, replaces, or deletes an admitted record."
readiness = ["ready"]

[[requirements]]
id = "atomic-batch"
path = "PRODUCT.md"
anchor = "5.3 Atomic visibility"
text = "The batch either admits every record or admits none."
readiness = ["ready"]

[[requirements]]
id = "global-order"
path = "PRODUCT.md"
anchor = "6.1 Linearizable cut"
text = "Concurrent publications may be ordered either way, but every observer agrees on the chosen BoardSeq order."
readiness = ["ready"]

[[requirements]]
id = "claim-winner"
path = "PRODUCT.md"
anchor = "7.2 Claim winner"
text = "The earliest valid Claim by BoardSeq is the unique winner for its claim key and generation."
readiness = ["blocked"]

[[requirements]]
id = "cursor-monotonic"
path = "PRODUCT.md"
anchor = "8.3 Cursor acknowledgement"
text = "Acknowledgement advances a cursor monotonically."
readiness = ["ready"]

[[requirements]]
id = "semantic-nonauthority"
path = "PRODUCT.md"
anchor = "9.2 Authority boundary"
text = "Semantic similarity never creates a causal link, claim, decision, correction, authorization, or task dependency."
readiness = ["blocked"]

[[requirements]]
id = "batch-authorization"
path = "PRODUCT.md"
anchor = "10.2 Capability evaluation"
text = "Every record in a batch must satisfy the capability independently."
readiness = ["ready"]

[[requirements]]
id = "movement-equivalence"
path = "PRODUCT.md"
anchor = "11.3 Live shard movement"
text = "No authorized retained record may be omitted or duplicated because movement is active."
readiness = ["ready"]

[[requirements]]
id = "checkpoint-replay"
path = "PRODUCT.md"
anchor = "12.1 Checkpoint"
text = "Restoring a checkpoint followed by replay above its cut produces the same logical state as full replay."
readiness = ["blocked"]

[[requirements]]
id = "recovery-equivalence"
path = "PRODUCT.md"
anchor = "13.2 Recovery result"
text = "Recovery either produces one state observationally equivalent to pre-crash acknowledged history or fails closed before serving traffic."
readiness = ["blocked"]

[[requirements]]
id = "storage-exclusivity"
path = "DEVELOPMENT.md"
anchor = "13.1 Storage transaction adapter"
text = "It provides guarded conditional insert or update with at most one successful contender per guard instance."
readiness = ["ready"]

[[requirements]]
id = "composition"
path = "DEVELOPMENT.md"
anchor = "14.3 Composition theorem"
text = "The whole-program theorem connects every public handler to one abstract transition or observation over `BoardState`."
readiness = ["blocked"]

[[requirements]]
id = "capacity"
path = "RESEARCH.md"
anchor = "6.1 Sustainable throughput"
text = "The release target is 5,000,000 admitted records per second across the reference deployment."
readiness = ["blocked"]

[[requirements]]
id = "agent-utility"
path = "RESEARCH.md"
anchor = "9.5 Agent utility"
text = "The study measures answer accuracy, follow-up query count, input tokens, wall time, and unsupported assertions."
readiness = ["blocked"]

[[dependencies]]
before = "immutable-record"
after = "atomic-batch"

[[dependencies]]
before = "storage-exclusivity"
after = "atomic-batch"

[[dependencies]]
before = "atomic-batch"
after = "global-order"

[[dependencies]]
before = "global-order"
after = "claim-winner"

[[dependencies]]
before = "global-order"
after = "cursor-monotonic"

[[dependencies]]
before = "global-order"
after = "movement-equivalence"

[[dependencies]]
before = "movement-equivalence"
after = "checkpoint-replay"

[[dependencies]]
before = "checkpoint-replay"
after = "recovery-equivalence"

[[dependencies]]
before = "claim-winner"
after = "composition"

[[dependencies]]
before = "recovery-equivalence"
after = "composition"

[[dependencies]]
before = "composition"
after = "capacity"

[[dependencies]]
before = "semantic-nonauthority"
after = "agent-utility"
