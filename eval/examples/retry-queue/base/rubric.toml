expected_approval = true
minimum_unresolved = 0
required_signal_groups = []
forbidden_terms = ["Kafka", "Redis", "Kubernetes"]
fixture_unresolved = []

[[requirements]]
id = "fifo"
anchor = "Queue operations"
text = "Jobs are dequeued in first-in, first-out order."
readiness = ["ready"]

[[requirements]]
id = "ownership"
anchor = "Queue operations"
text = "At most one worker owns an unacknowledged job at a time."
readiness = ["ready"]

[[requirements]]
id = "recovery"
anchor = "Recovery"
text = "After a worker crash, its unacknowledged job becomes available again during recovery."
readiness = ["ready"]

[[requirements]]
id = "retry-policy"
anchor = "Retry policy"
text = "A failed job is attempted at most three times and is then moved to a dead-letter queue."
readiness = ["ready"]

[[dependencies]]
before = "ownership"
after = "recovery"

