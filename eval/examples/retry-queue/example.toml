id = "retry-queue"
title = "An underspecified retry policy must not block contractible queue work"
relation = "localized-block"
affected_requirements = ["retry-policy"]

[[scenarios]]
id = "base"
directory = "base"

[[scenarios]]
id = "variant"
directory = "missing-criterion"
