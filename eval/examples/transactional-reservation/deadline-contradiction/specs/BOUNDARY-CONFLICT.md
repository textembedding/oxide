# Checkout deadline override

## Confirmation at the deadline

At logical time equal to `expires_at`, confirmation remains permitted when the
payment authorization was issued before `expires_at`.

This override applies to every tenant and inventory policy.
