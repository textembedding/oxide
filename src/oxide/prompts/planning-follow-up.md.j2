{% if kind == "schema-repair" %}
Your proposed structured roadmap value failed mechanical schema validation. Exact error:
{{ problem }}. Repair only the structured `roadmap` object; preserve all source-derived product
semantics and return the complete response envelope again. Oxide renders the repository artifact;
return only the structured response. Do not ask the user to resolve your encoding error.
{% elif kind == "trace-repair" %}
Your proposed structured roadmap failed deterministic source-trace or maintenance qualification.
Exact error: {{ problem }}. Repair the cited source anchor and exact requirement, dependency, or
scoped-maintenance change in the `roadmap` object using only the frozen source bundle and approved
baseline. Preserve all source-derived product semantics and return the complete response envelope
again. Oxide renders the repository artifact; return only the structured response. Do not ask the
user to resolve a qualification error that the supplied sources can resolve.
{% elif kind == "missing-roadmap" %}
You omitted the required structured `roadmap` object. Return the complete mechanically valid
response envelope without changing source-derived product semantics. Oxide renders the repository
artifact; return only the structured response.
{% elif kind == "user-feedback" %}
User feedback:
{{ feedback }}
Known mechanical issue:
{{ problem }}
Revise the full proposal.
{% elif kind == "approval-denied" %}
The user attempted approval, but the proposal is not contractible. Mechanical issue:
{{ problem }}. Resolve all reported gaps without silently inventing semantics.
{% else %}
{{ fail_unknown_follow_up_kind }}
{% endif %}
